# Requirements Triage

This triage consolidates `UNCLEAR` items from `specs/spec-coverage.md` and feature/data/architecture specs. Repeated unclear statements are grouped into one requirement when they describe the same product decision.

| ID | Area | Unclear requirement / question | Current observed behavior | Evidence files/tests | Risk if left unclear | Proposed answer | Bucket | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RT-001 | Sync conflicts | Resolved conflict-resolution policy for concurrent transaction-month and whole-file edits. | Transactions merge by id; whole-file cached writes use last writer wins and emit a visible warning when a newer remote file is overwritten. | `specs/spec-coverage.md`; `specs/architecture.md`; `specs/features/storage-sync.md`; `src/helpers/sync.ts`; `src/helpers/__tests__/sync.spec.ts` | Silent data loss or user distrust when multi-device edits collide. | Use "last writer wins with visible warning" for whole-file conflicts; keep transaction merge-by-id for queued transaction rows until a richer conflict UI exists. | Implemented | Medium |
| RT-002 | Sync failure UX | Resolved shared sync failure behavior for sync upload, local queue, and IndexedDB write failures. | Pending `to_sync` records remain queued, failed sync attempts set shared sync-failed state and emit a persistent toast, and the sync status action remains available for retry. | `specs/spec-coverage.md`; `specs/features/storage-sync.md`; `specs/features/transactions.md`; `specs/features/budget.md`; `src/helpers/files.ts`; `src/helpers/sync.ts`; `src/stores/storage.ts`; `src/layout/AppTopbar.vue`; `src/helpers/__tests__/sync.spec.ts`; `src/stores/__tests__/storage.spec.ts` | Users may believe changes are saved remotely when they are only local, or may not know how to retry. | Keep pending items queued, show a persistent sync-failed status/toast, and provide retry from sync status. | Implemented | Medium |
| RT-003 | Account deletion and references | Account lifecycle must preserve historical account ids. Archive/hide is the normal path, and hard deletion is blocked in all cases. | Implemented: account UI exposes hide/unhide instead of hard delete, and store-level hard deletion returns `false` without writing `accounts.json`. | `specs/data-model.md`; `specs/features/accounts.md`; `specs/product-overview.md`; `src/stores/accounts.ts`; `src/stores/__tests__/accounts.spec.ts` | Historical reports, budgets, values, and balances can become incomplete or misleading if account keys are removed. | Use `hideSince` to archive/hide accounts. Do not expose or execute hard deletion from normal app flows, even when no references are known. | Implemented | High |
| RT-004 | Versionless compatibility | What compatibility policy applies to persisted JSON file names and structures? | Current files are simple versionless JSON shapes. Store loaders normalize persisted shapes through `src/helpers/persistedShapes.ts`; all persisted file families have tests for current names, additive defaults, or ignored deprecated structures. | `specs/data-model.md`; `specs/architecture.md`; `src/helpers/persistedShapes.ts`; `src/helpers/__tests__/persistedShapes.spec.ts`; store load tests under `src/stores/__tests__/` | Accidental file renames or shape replacements can break existing personal finance data. | Keep JSON files versionless. Existing file names and top-level structures are compatibility contracts and renames/replacements are prohibited. New structures must be additive and have default data on read. Removed/deprecated structures must be ignored when present. | Implemented | High |
| RT-005 | Invalid JSON / malformed file handling | Resolved invalid persisted-file behavior for unparseable JSON, missing required fields, and unsupported enum values. | Implemented for the shared persisted-file path: provider parse failures raise recoverable invalid-file metadata, invalid remote reads block automatic same-file writes, and malformed `accounts.json` entries are rejected before account state is replaced. | `specs/data-model.md`; `specs/features/accounts.md`; `src/helpers/persistedFileErrors.ts`; `src/helpers/files.ts`; `src/helpers/storage/dropbox.ts`; `src/helpers/storage/http_server.ts`; `src/helpers/persistedShapes.ts`; `src/stores/accounts.ts`; `src/helpers/__tests__/files.spec.ts`; `src/helpers/__tests__/httpServer.spec.ts`; `src/stores/__tests__/accounts.spec.ts` | App startup or reports may fail unpredictably; users may lose access to recoverable data. | Invalid files must not be overwritten automatically; show a recoverable error and keep the raw remote file intact until user action. | Implemented | Medium |
| RT-006 | Legacy account types | Public seed account types must use current `AccountType` values. | Implemented: `public/accounts.json` uses current `AccountType` values for seeded accounts, and the account loader continues to reject unsupported enum values. | `specs/data-model.md`; `specs/features/accounts.md`; `specs/spec-coverage.md`; `public/accounts.json`; `src/stores/accounts.ts` | Future manual seed edits that use unsupported type strings will be rejected by account loading. | Keep `public/accounts.json` aligned to current `AccountType` values; do not add legacy string tolerance unless product support for old persisted files is explicitly required. | Implemented | High |
| RT-007 | Config defaults | Resolved minimum `config.json` shape and missing-config seeding behavior. | Implemented: config loading normalizes missing `stock_api` and `inv_composition` to empty objects in memory, and first successful storage initialization creates a missing `config.json` with the minimum persisted shape without overwriting existing or invalid files. | `specs/data-model.md`; `specs/features/settings.md`; `src/stores/config.ts`; `src/stores/storage.ts`; `src/helpers/persistedShapes.ts`; `src/stores/__tests__/config.spec.ts`; `src/helpers/__tests__/persistedShapes.spec.ts`; `src/stores/__tests__/storage.spec.ts` | Settings, values sync, and portfolio composition can behave inconsistently on first run. | Minimum persisted shape is `{ "stock_api": {}, "inv_composition": {} }`; create it when `config.json` is missing on first successful storage initialization, without overwriting valid or invalid existing files. | Implemented | Medium |
| RT-008 | Transaction identity | Resolved transaction edit identity policy. | Editing queues deletion of the original transaction and saves a new transaction with a fresh `Date.now()` id. Code currently satisfies this requirement. | `specs/features/transactions.md`; `specs/data-model.md`; `src/components/TransactionEditDialog.vue`; `src/stores/__tests__/transactions.spec.ts`; `src/helpers/__tests__/sync.spec.ts` | Future audit, reconciliation, or sync conflict behavior may be harder if identities are not stable. | Continue using delete-plus-new-id for edits. Transactions are mutable entries without stable audit identity; preserving ids on edit is out of scope unless a future audit/reconciliation feature changes the model. | Implemented | Medium |
| RT-009 | Local queue write failure | Resolved local IndexedDB queue write failure behavior for transaction and budget edits. | Implemented: transaction and budget saves await local queue writes before closing/resetting or mutating saved state, failed local queue writes emit an error toast message, and pending/saved state is not advanced for the failed write. | `specs/features/transactions.md`; `specs/features/budget.md`; `src/stores/transactions.ts`; `src/stores/budget.ts`; `src/components/TransactionEditDialog.vue`; `src/components/TransactionExpenseDialog.vue`; `src/components/TransactionTransferDialog.vue`; `src/views/Budget.vue`; `src/stores/__tests__/transactions.spec.ts`; `src/stores/__tests__/budget.spec.ts`; `src/components/__tests__/TransactionEditDialog.spec.ts`; `src/helpers/__tests__/idb.spec.ts` | Users may leave the page believing an edit was saved when it was rejected locally. | Treat local queue failure as blocking: show an error toast/dialog, keep the form open or pending, and do not mark the edit as saved. | Implemented | Medium |
| RT-010 | Balance freshness and recalculation | Should balance snapshots be disposable cache, durable financial records, or user-rebuildable derived data? | Balances are calculated and persisted; current month may be recalculated; no user-facing stale/failure state or force-recalculate flow is specified. | `specs/data-model.md`; `specs/features/balance.md`; `specs/spec-coverage.md`; `src/stores/balance.ts`; `src/stores/__tests__/balance.spec.ts` | Reports can show stale net worth without a clear recovery path. | Document balances as derived cache and add a force-recalculate action for authenticated users, with warnings when required source data is missing. | Code change likely | Medium |
| RT-011 | Missing exchange rates | Should missing/stale exchange rates display a warning, exclude rows, or display partial totals? | Expense/investment conversions may underrepresent totals; no explicit UI error is specified. | `specs/features/expenses.md`; `specs/features/investments.md`; `specs/features/values.md`; `src/stores/values.ts`; `src/stores/__tests__/values.spec.ts` | Financial totals can look authoritative while omitting unconverted amounts. | Display partial totals with a visible "missing rate" indicator listing affected currencies/accounts; do not silently coerce to zero in UI summaries. | Code change likely | Medium |
| RT-012 | Value provider failures | Should external value sync failures be shown per row, per provider, or as one generic error? | External API errors are not surfaced consistently; provider details are not shown by the values view. | `specs/features/values.md`; `src/views/Values.vue`; `src/stores/config.ts`; `src/stores/__tests__/values.spec.ts` | Users may rely on stale prices/rates after a failed sync. | Show provider-level failure summaries plus row-level stale indicators when a specific asset/currency was not updated. | Code change likely | Low |
| RT-013 | Values fallback window | What fallback window should the product guarantee for stale rates/prices? | `getValue()` searches prior months only up to `maxLevels`; product-level freshness guarantee is unspecified. | `specs/features/values.md`; `src/stores/values.ts`; `src/stores/__tests__/values.spec.ts` | Reports can use stale rates without users knowing how stale they are. | Document the current fallback limit as an implementation rule, then add a freshness warning when the selected period uses a prior-month value. | Test only | Medium |
| RT-014 | Dashboard empty/error state | Should the dashboard show an empty state, skeleton, or error if current account/balance data is unavailable? | Dashboard returns empty arrays when no balance data exists; no explicit UI error state is specified. | `specs/features/dashboard.md`; `src/views/HomeView.vue`; `src/stores/__tests__/balance.spec.ts` | Home screen can appear blank or misleading during load/failure. | Document a read-only empty state: "No current balance data available"; reserve error state for failed load once error handling exists. | Code change likely | Medium |
| RT-015 | Dashboard currency totals | Should dashboard totals ever convert currencies, or must they remain split by currency? | Dashboard totals are separated by currency and not converted. | `specs/features/dashboard.md`; `src/views/HomeView.vue`; `src/components/AccountValueCard.vue` | Product expectations for "total" may conflict with current per-currency cards. | Document current split-by-currency behavior as intended for dashboard cards; use converted totals only in reports that already depend on global currency. | Document only | High |
| RT-016 | Income visibility on expenses | Should income be hidden from unauthenticated users because it is sensitive, or because current loading requires authentication? | `/expenses` is unprotected; unauthenticated users see expenses only, authenticated users see income and expenses. | `specs/features/expenses.md`; `specs/features/authentication.md`; `src/views/Expenses.vue`; router spec | Privacy model may be inconsistent if income is intentionally sensitive but other cached data leaks through reports. | Document income as sensitive and visible only after local authentication; add rendered tests for authenticated/unauthenticated visibility. | Test only | Medium |
| RT-017 | Investment missing metadata | Should investment accounts with missing class allocation be blocked, excluded, or grouped as unknown? | Missing investment grouping data generally returns empty arrays; no explicit UI error is specified. | `specs/features/investments.md`; `specs/features/accounts.md`; `src/helpers/investments.ts`; `src/helpers/__tests__/investments.spec.ts` | Portfolio allocations and expected-vs-actual views can be incomplete without warning. | Enforce required allocation in account setup for investment accounts; analytics should group legacy missing values under "Unknown" with a warning. | Code change likely | Medium |
| RT-018 | Settings retry-login errors | Should retry-login failures provide generic or provider-specific remediation text? | Retry-login failures do not show a dedicated error toast in the spec. | `specs/features/settings.md`; `src/views/Settings.vue`; `src/stores/storage.ts`; `src/stores/__tests__/storage.spec.ts` | Users may be stuck offline/logged out without next steps. | Show provider-specific retry failure toast when available; otherwise show a generic retry-later message and keep existing state unchanged. | Code change likely | Medium |
| RT-019 | Composition persistence | Should zero-valued generated investment-composition cells be persisted or omitted? | Saving appears to write generated zero-valued weights for every generated region/type cell. | `specs/features/settings.md`; `src/views/Settings.vue`; `src/stores/config.ts`; `src/stores/__tests__/config.spec.ts` | Config files can become noisy; future defaults may be harder to distinguish from explicit zeroes. | Omit zero-valued cells unless the user explicitly needs hard zero targets; document normalization/read behavior to treat missing as zero. | Code change likely | Low |
| RT-020 | Authentication failure UX | What should users see when WebAuthn, provider login, or Dropbox token refresh fails? | WebAuthn errors are logged or uncaught; UI does not present detailed provider/auth error messages. | `specs/features/authentication.md`; `specs/spec-coverage.md`; `src/components/Auth.vue`; `src/helpers/storage/dropbox.ts`; `src/helpers/__tests__/httpServer.spec.ts` | Users may be locked out without understanding recovery options. | Define a shared auth error state with retry, reset local credentials, and provider-login restart actions. | Code change likely | Medium |
| RT-021 | WebAuthn requirement | Should local WebAuthn be mandatory for all non-local providers, or optional? | Storage login is separate from local device authentication; HTTP server bypasses WebAuthn; non-local provider flow expects local credential registration/authentication. | `specs/features/authentication.md`; `specs/architecture.md`; `src/components/Auth.vue` | Security and usability expectations differ sharply depending on whether WebAuthn is required. | Keep WebAuthn mandatory for Dropbox-backed sensitive routes; document HTTP server bypass as development-only. | Decision needed | Medium |
| RT-022 | Lost local credential recovery | What recovery path should exist when local credentials are lost but provider credentials still exist? | Credential metadata is stored in browser local storage; reset clears `crlocal`; recovery behavior is not specified. | `specs/features/authentication.md`; `src/components/Auth.vue`; `src/stores/storage.ts`; `src/stores/__tests__/storage.spec.ts` | Users may be unable to access data from a valid provider session. | Allow reset/re-register local credentials after successful provider login, with clear warning that this only resets the local gate. | Code change likely | Medium |
| RT-023 | First-run onboarding | What is the intended first-run flow after storage login and default `accounts.json` seeding? | Missing `accounts.json` is seeded from `public/accounts.json`; no user-facing onboarding flow is specified. | `specs/product-overview.md`; `specs/features/authentication.md`; `src/components/Auth.vue`; `public/accounts.json` | Users may start with confusing defaults or incomplete setup. | Document a first-run checklist: storage login, seed accounts, register local credential, review/edit accounts, confirm base currency/config. | Code change likely | Low |
| RT-024 | Offline product promise | What offline behavior does the product promise: app shell only, cached read-only data, queued edits, or full workflows? | PWA assets are offline-ready; finance JSON depends on provider state and IndexedDB cache; queued edits exist for some flows. | `specs/spec-coverage.md`; `specs/features/pwa.md`; `specs/features/storage-sync.md`; `vite.config.ts`; `src/helpers/pwa.ts`; `src/helpers/idb.ts` | Users may assume full offline finance functionality when only cached app shell/data is reliable. | Document: offline supports app shell plus previously cached data and queued local edits for supported write flows; fresh remote data requires connectivity. | Decision needed | Medium |
| RT-025 | Service-worker runtime behavior | How should generated service-worker behavior and registration failures be specified/tested? | Source specs describe VitePWA config and update toast; generated runtime output is not inspected; registration failures are not handled in app code. | `specs/features/pwa.md`; `specs/spec-coverage.md`; `vite.config.ts`; `src/helpers/pwa.ts`; `src/helpers/__tests__/pwa.spec.ts` | PWA behavior can regress across build/plugin changes without clear expectations. | Add generated-manifest/cache assertions to build tests where practical; document registration failure as console-only until product wants user-facing PWA errors. | Test only | Medium |
| RT-026 | PWA update dismissal | Should users be allowed to dismiss an update prompt without updating? | App shows a persistent toast and clicking "Update now" calls `updateServiceWorker(true)`; dismiss policy is not specified. | `specs/features/pwa.md`; `src/App.vue`; `src/helpers/pwa.ts` | Persistent update prompts may interrupt users during finance entry; dismissible prompts may leave old code running. | Keep update prompt persistent but non-blocking; allow normal toast close only if data-entry flows are not interrupted. | Decision needed | Low |
| RT-027 | Local dev server `.tmp` handling | Should the local server create `.tmp/` automatically when missing? | Missing `.tmp/` can make `/list` fail; server does not create it according to spec. | `specs/features/local-dev-server.md`; `server/index.js`; `src/helpers/__tests__/httpServer.spec.ts` | Local development setup can fail with avoidable filesystem errors. | Create `.tmp/` on server startup or before list/write operations; document it as development-only behavior. | Code change likely | Medium |
| RT-028 | Local dev server path safety | Should the local server reject nested or malicious JSON paths? | JSON writes matching `/*.json` write request body under `.tmp/<path>`; path traversal expectations are not specified. | `specs/features/local-dev-server.md`; `server/index.js`; missing server integration tests | A development server could overwrite unintended files if path handling is unsafe. | Reject path traversal and normalize to safe `.json` file names under `.tmp/`; add server-level tests. | Code change likely | Medium |
| RT-029 | Google Drive provider scope | Should Google Drive be treated as planned/unavailable or an implemented provider? | Google Drive appears in provider options as planned/unavailable and cannot be selected. | `specs/spec-coverage.md`; `specs/features/storage-sync.md`; `src/helpers/storage/index.ts`; `src/helpers/__tests__/storageIndex.spec.ts` | Product/spec readers may assume support exists and build flows around it. | Document Google Drive as out of scope until a provider helper, auth flow, and sync tests exist. | Document only | High |
| RT-030 | Specs versus implementation detail | How should specs distinguish immutable product requirements from reverse-engineered current implementation details? | Specs use `CONFIRMED`, `INFERRED`, and `UNCLEAR`, but no formal separation between product contract and observed implementation exists. | `specs/spec-coverage.md`; `specs/feature-template.md`; all feature specs | Future contributors may preserve accidental implementation details as if they are product requirements. | Add a convention: `Product Contract` for intended behavior and `Observed Implementation` for current code traceability in new or revised specs. | Document only | Medium |
| RT-031 | Component/integration coverage | What rendered component, provider integration, generated PWA, and end-to-end tests are required before specs are fully testable? | Current coverage is strongest at store/helper level; rendered component and integration coverage is missing for most feature specs. | `specs/spec-coverage.md`; feature `Missing Tests / Coverage Gaps`; existing `src/**/__tests__` suites | Specs may look testable while key user flows remain unverified. | Write `specs/testing-strategy.md` defining required component, integration, and manual coverage per feature before broad UI/code changes. | Document only | High |
| RT-032 | Error criteria standard | What shared error-handling criteria should specs use when product behavior is undefined? | Error criteria are intentionally incomplete where product has not defined failure states. | `specs/spec-coverage.md`; feature `Error Handling` sections | Teams may implement inconsistent toasts, silent failures, or blocking dialogs. | Create `specs/validation-and-errors.md` with default severity, toast/dialog, retry, and logging rules; features can override it. | Decision needed | High |
